name: exa
identifier: exa
description: 具有颜色高亮的ls替代品
macos:
  need_restart: false
  dependencies:
  - cargo
  install_tasks:
  - !InstallTask
    name: exa
    installer: cargo
  uninstall_tasks:
  - !UninstallTask
    name: exa
    installer: cargo
  check_tasks:
  - !CheckTask
    name: exa
    installer: cargo
windows:
  need_restart: false
  dependencies:
    - cargo
  install_tasks:
    - !InstallTask
      name: exa
      installer: cargo
  uninstall_tasks:
    - !UninstallTask
      name: exa
      installer: cargo
  check_tasks:
    - !CheckTask
      name: exa
      installer: cargo
linux:
  need_restart: false
  dependencies:
    - cargo
  install_tasks:
    - !InstallTask
      name: exa
      installer: cargo
  uninstall_tasks:
    - !UninstallTask
      name: exa
      installer: cargo
  check_tasks:
    - !CheckTask
      name: exa
      installer: cargo
