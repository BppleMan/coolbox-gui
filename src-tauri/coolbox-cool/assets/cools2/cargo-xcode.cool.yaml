name: cargo-xcode
identifier: cargo-xcode
description: cargo子命令用于生成xcode project
macos:
  need_restart: false
  dependencies:
  - cargo
  install_tasks:
  - !InstallTask
    name: cargo-xcode
    installer: cargo
  uninstall_tasks:
  - !UninstallTask
    name: cargo-xcode
    installer: cargo
  check_tasks:
  - !CheckTask
    name: cargo-xcode
    installer: cargo
windows:
  need_restart: false
  dependencies:
    - cargo
  install_tasks:
    - !InstallTask
      name: cargo-xcode
      installer: cargo
  uninstall_tasks:
    - !UninstallTask
      name: cargo-xcode
      installer: cargo
  check_tasks:
    - !CheckTask
      name: cargo-xcode
      installer: cargo
linux:
  need_restart: false
  dependencies:
    - cargo
  install_tasks:
    - !InstallTask
      name: cargo-xcode
      installer: cargo
  uninstall_tasks:
    - !UninstallTask
      name: cargo-xcode
      installer: cargo
  check_tasks:
    - !CheckTask
      name: cargo-xcode
      installer: cargo
