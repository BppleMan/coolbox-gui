name: flutter
identifier: flutter
description: 一个UI框架
macos:
  need_restart: false
  dependencies:
  - homebrew
  install_tasks:
  - !InstallTask
    name: flutter
    installer: brew
  uninstall_tasks:
  - !UninstallTask
    name: flutter
    installer: brew
  check_tasks:
  - !CheckTask
    name: flutter
    installer: brew
windows:
  need_restart: false
  dependencies:
    - homebrew
  install_tasks:
    - !InstallTask
      name: flutter
      installer: brew
  uninstall_tasks:
    - !UninstallTask
      name: flutter
      installer: brew
  check_tasks:
    - !CheckTask
      name: flutter
      installer: brew
linux: null
