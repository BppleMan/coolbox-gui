name: "cargo-xcode"
description: "cargo子命令用于生成xcode project"
dependencies:
  - "cargo"

install_tasks:
  - !InstallTask
    installer: "cargo"
    name: "cargo-xcode"

uninstall_tasks:
  - !UninstallTask
    installer: "cargo"
    name: "cargo-xcode"

check_tasks:
  - !CheckTask
    installer: "cargo"
    name: "cargo-xcode"
